import React from 'react';

const Author = () => {
    return (
        <div className="flex flex-col items-center justify-center min-h-screen">
            This will later be its own page where people can interact with the author cards by buying it, downloading the books, and interacting with it if they own it, much like that of an NFT marketplace when you click on a particular one. Not to worry about for now though, as we don't know what it'll be.
        </div>
    );
};

export default Author; 