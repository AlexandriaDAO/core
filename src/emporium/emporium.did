type Nft = record {
  status : NftStatus;
  token_id : nat;
  owner : principal;
  time : nat64;
  price : nat64;
};
type NftStatus = variant { Listed; Unlisted; Reimbursed };
type Result = variant { Ok : text; Err : text };
service : {
  buy_nft : (nat) -> (Result);
  cancel_nft_listing : (nat) -> (Result);
  get_listing : () -> (vec record { text; Nft }) query;
  list_nft : (nat, nat64) -> (Result);
  update_nft_price : (text, nat64) -> (Result);
}
