import NftDisplay from './NftDisplay';
import NftFooter from './NftFooter';
import type { NftDisplayProps, NftFooterProps } from './types';

export {
  NftDisplay,
  NftFooter
};

export type {
  NftDisplayProps,
  NftFooterProps
};

export default NftDisplay; 