type Result = variant { Ok : text; Err : text };
service : {
  abe_decrypt : (text) -> (Result);
  encrypted_ibe_decryption_key : (blob) -> (text);
  encryption_key : () -> (text);
  greet : () -> (text) query;
  wbe_decrypt : (text) -> (Result);
}
